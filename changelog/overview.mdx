---
title: "Changelog"
description: "Product updates and improvements"
---

<Update label="June 6, 2026">

## New features

**Public lore archive**

The Portal homepage is now a public reading surface. Visitors who aren't signed in see your published lore at `/` and can open individual entries at `/public/lore/{id}` without an account. Anything you mark as shared in AllCodex Core shows up here automatically.

**Owner-gated Portal**

Writer tools (dashboard, brain dump, entities, AI views, settings) are now gated to the configured AllKnower owner account. Unauthenticated visits to private pages redirect to `/login?next=...`, and unauthenticated requests to private APIs return `401 LOGIN_REQUIRED`. Public lore routes and the login surface remain open.

**Owner login page**

A dedicated `/login` page collects the AllKnower URL, email, and password, then sets session cookies on success. Login only succeeds if the AllKnower session belongs to the owner account—non-owner sessions are rejected with `403 FORBIDDEN`.

## Updates

**Shared view publication status**

The shared-content view now shows an explicit **Published** or **Not Shared** indicator per entry, so you can tell at a glance which lore is reachable from the public archive.

**Share URLs link straight to the destination**

Share links now point directly to the destination URL instead of going through a constructed redirect path.

## Removed

**Self-service sign-up disabled**

Portal registration is no longer available. The Settings page no longer renders the AllKnower register form, and `POST /api/auth/register` now returns `403 FORBIDDEN` with the message _"Sign-up is disabled. Use the owner account."_ Provision accounts through AllKnower directly.

</Update>

<Update label="June 3, 2026">

## New features

**Relationship graph**

The Portal relationship view now renders with React Flow instead of Mermaid, giving you smoother pan-and-zoom, cleaner layouts, and consistent relation colors across the app.

**Push notifications**

Subscribe to push notifications from the Portal and get alerted when long-running AI jobs, suggestions, and shared updates land. Look for the new notification bell in the top navigation.

**Streaming AI responses**

AI responses now stream token-by-token in brain dump and entity views, so you see results as they're generated instead of waiting for the full reply.

**Auto-relate on brain dump**

When you save a brain dump, the system now automatically proposes relationships to existing entities. Suggestions are cached per content hash and deduplicated in flight, so repeat work is instant.

## Updates

**Hybrid search**

Search now blends keyword and vector results using Reciprocal Rank Fusion, surfacing more relevant matches—especially for paraphrased or conceptual queries.

**Relation suggestions cache**

Suggestion responses are now cached and invalidated by content hash, with inflight deduplication. Expect faster repeat suggestions and lower latency on busy pages.

**AllCodex Core clarified as server-only**

We've updated the [AllCodex Core](/quickstart) framing to make it clear that Core is a server-only fork (no desktop client), runs on Node 22, and ships as a fixed set of packages.

## Fixes

- Fixed relation color sync between AllKnower and the Portal so colors stay consistent across views.
- Fixed a streaming entity parsing issue and tightened Zod null coercion to prevent intermittent parse errors.
- Fixed statblock rendering glitches in entity views.
- Scoped Phase 5 graph queries by `userId` to prevent cross-tenant data exposure.
- Tightened rate limiter scoping and hardened integration tests.

</Update>
